# Harbor · 多浏览器管理器

> 当前发布目标：v0.3.8 预览版。新增导航隔离、默认关闭的 Thinking/归档实验、Windows 更新提醒与一键更新、Mac 源码启动。下方保留历史开发记录；当前能力和限制以发布仓库 README.md / RELEASE_NOTES.md 为准。真实 Arena 和 Mac 仍需验收。

当前管理器提供 **鹈鹕动画测试**、**鹈鹕测试快速版**、**火柴人大战** 与 **纯 HTML + CSS 复刻** 四种自动任务。三题数字测试的启动入口、执行逻辑、联合解析、人工补录及重新解析接口已移除。

## 当前版本：账号与环境分离

账号独立保存邮箱与 DPAPI 加密密码，一个账号可关联多个环境。凭据保存当前仅支持 Windows。旧环境中的凭据在下次启动时事务性迁移至 `accounts` 表，迁移不解密密码，原环境改为关联账号；不会合并同邮箱的旧环境，避免覆盖不同密码。旧环境保留原无痕类型；无凭据的旧环境需要在编辑中关联账号。

### 使用流程

1. 停止旧任务并退出管理器，重启**主项目源码版**并刷新页面。已有 EXE/ZIP 没有自动更新。
2. 打开侧栏「账号管理」，添加邮箱和密码，或使用自动迁移的账号。
3. 点击账号旁的「新建保留环境」或「新建无痕环境」，确认名称和类型后「创建并启动」。也可在环境页只创建配置、稍后启动。
4. 保留型使用 `session-profiles/<环境ID>` 独立用户目录。再次打开先核验保存邮箱的登录身份，未确认时尝试自动登录。Cookie 仍可能过期，网站也可能要求验证码。
5. 临时无痕型使用全新进程及非持久化 BrowserContext。通过管理器关闭、关闭浏览器窗口或最后一个标签页后，等待任务记录收尾，自动删除环境配置和列表项。账号、加密凭据、历史记录及截图保留。再次使用时请从账号管理新建环境；只关闭某个结果标签页而其他标签仍开着时，不丢弃环境。
6. 登录确认后在选定环境中启动现有自动任务。新建或打开浏览器不会直接发送题目。

### 关闭、删除和账号修改

- 关闭保留型环境不会删除 profile；关闭临时型会丢弃会话和环境配置，并从列表移除。账号及历史任务都保留。
- 删除环境需先关闭浏览器并确认，将删除当前版本该环境的 `session-profiles` 目录和配置，不删除账号、任务记录和截图。物理删除不是安全擦除，备份可能保留资料。
- 旧版 `profiles` 从不自动用于新环境，删除环境时如有旧 profile 则归档到 `archive`；迁移不删除这些数据。
- 删除账号前必须先删除关联环境，以免留下失去凭据的环境。删除按钮在有关联时禁用，API 同样拦截。
- 编辑账号前须关闭全部关联环境。密码留空保留，可以更新密码；已有环境时不能更换邮箱，应新建账号。环境创建后不更换账号或类型，防止混用 profile；需要不同组合时新建环境。
- 一个账号可关联多个环境，同时启动仍受管理器总上限和网站规则约束；不提供风控规避或验证码自动处理。
- 历史记录继续按原环境保存，因此删除环境后记录仍可查阅，但无法通过原环境入口直接打开对话。

### 验证限制

凭据仍仅向受信任的 Arena 域名填入。身份核验使用可选会话接口和页面账号证据，识别不确定时可按文末规则人工核对确认。没有“未显示登录按钮就视为成功”的回退。自动登录会先检查已有邮箱表单或可见登录按钮；未找到时等待页面加载，并尝试展开侧边栏后查找登录入口。只尝试展开一次，避免反复切换。遇验证码请在环境窗口完成人工操作，再点击「检查登录」。

本次未运行测试、真实登录、发送题目或重新打包。首次实机运行需人工确认数据迁移、Windows DPAPI、保留型重开以及当前网站登录接口兼容性。下方旧功能说明若涉及仅无痕模式，以本节为准。

## 启动和更新

在 `arena-modeltrace-bot` 目录执行：

```powershell
pip install -r requirements.txt
python -m playwright install chromium
python browser_manager.py
```

在同一电脑访问 **http://127.0.0.1:8766**。需要 Python 3.10+ 和本机桌面环境。

更新前先停止正在运行的任务，保存浏览器中的工作，Ctrl+C 退出旧管理器；随后重新启动并刷新页面。管理器退出会尝试关闭受管浏览器。不要以多 worker 或 reload 模式启动。

## 浏览器环境

- 创建、编辑、搜索、移除环境；新增环境放在列表顶部。
- 每个环境可选保留型或临时无痕型，分别复用独立 profile 或每次创建全新会话。
- 在账号管理中保存 Arena 邮箱和密码，再由环境关联账号；打开环境时检查并按需自动登录。凭据仅支持 Windows DPAPI，不支持的平台拒绝保存，绝不退回明文。
- 支持单个及批量启停，默认最多同时运行 6 个环境。
- 编辑和移除前须关闭浏览器；移除环境会归档 profile，不永久清除登录资料。
- 新管理器不再支持插件加载。插件开关、目录依赖和任务限制已移除，浏览器显式禁用扩展。旧数据库字段仅为兼容保留，不再生效。
- 数据隔离不等于指纹伪装，IP、系统信息及操作系统权限并不隔离。

## 自动任务

环境行点击 **「启动」**，管理器按类型打开环境，检查并按需自动登录。状态为「登录已确认」后点击 **「自动任务」**，手动授权开始本轮。登录及任务使用同一个 BrowserContext；启动浏览器本身不会发送题目。

验证码、二次验证或控件不匹配时保留浏览器，显示「等待人工处理」。在该环境窗口处理后点击「检查登录」，不会自动重试密码或绕过验证。当前通过 Arena 同源 `/api/auth/session` 返回的 `user.email` 与保存邮箱匹配确认身份；接口不可用或结构变化时保守阻止任务，需要适配，不能把“没有登录按钮”当作成功。此接口和控件尚未进行真实网站验证。

### 鹈鹕动画测试

固定题目：

```text
创建一个HTML，内容是SVG绘制一个鹈鹕骑自行车的2D动画，你不需要任何测试
```

### 纯 HTML + CSS 复刻

固定题目：

```text
https://cdn.jsdelivr.net/gh/AustinSuun/image/img/20260911135653908.png

用纯 HTML + CSS 复刻这张图，不许用图片，不许用 SVG。
```

参考图以链接形式发送，不下载或上传图片附件。目标模型能否读取链接取决于网站与网络；输出是否遵守禁用图片/SVG的要求，由人工审核，不冒充自动验证。

### 调度与停止

- 总任务数 1–50，每项对应一个独立对话。
- 最小间隔 3–3600 秒，默认 15，分别限制开页和发送频率。
- 并行数 1–3，默认 1；开页、发送和等待回复全程占用名额。
- 回复完成并通过稳定观察后才释放名额，已完成标签页保留，不计入执行中数量。
- 上限按环境、按本轮计算，不覆盖旧轮次或手动启动的网页生成。
- 关闭面板不会停止任务。「停止任务」停止调度和跟踪，不撤回已发送内容，也不自动取消网站生成；再次开始前需手动停止旧回复或等其完成。
- 输入和前台激活共享交互锁，避免多个任务争抢焦点。
- 每题仅点击一次发送。发送不确定、输入或导航异常时停止本轮，不自动重发。
- 回复完成依赖新增助手内容、生成结束信号、可用输入框及 30 秒稳定观察。15 分钟未确认完成会显示待检查并保留名额，不因超时继续开页。

## 思考展示不再触发自动操作

思考检测、因思考自动停止/归档以及相关诊断功能已移除。无论页面是否展示 Thinking/思考内容，任务都会按正常流程等待回复完成；不会因此停止生成或归档对话。

正常的生成中状态与回复完成判断仍保留，用于维持并行限制。需要中止时请使用管理器的手动「停止任务」；它只停止调度/跟踪，不会替你点击网站的停止生成按钮。

## 任务记录与人工审核

侧栏 **「任务记录 / 审核」**，或任务明细中的 **「记录 / 审核」**。

保存环境快照、题目、原始提取回答、对话 URL、状态、事件、错误原因和可选截图。原文最多保存 1,000,000 字符，超长显示截断标记。截图保存当前视口，不自动下载网站输出文件。

支持待审核、优质、一般、不合格评级，收藏、标签、备注、搜索和筛选。机器任务更新不会覆盖人工评价。可查看原文、截图、下载单条 JSON，在所属环境中打开原对话（需要该环境仍存在且已启动）。回答按文本展示，不执行其中的 HTML 或脚本。

记录随进度写入 SQLite，重启不丢失。重启时未完成记录标为中断，不自动重发。截图与对话内容可能包含敏感信息，仅存本机，请勿公开或提交到 Git。

### 已移除功能的历史数据

历史数字测试记录、已有分析数据、补录内容及人工评价**没有删除**。它们作为历史数据保留，可查看原文、已有附加数据、评价和导出；补录数据仍包含在 JSON 导出中，但不再提供新增补录或重新解析功能。

旧环境若保存了已停用任务类型，管理器显示时回退到鹈鹕类型，不自动启动或迁移旧任务。无需删除数据库。

## 文件与安全

- `browser_manager.py`：环境管理和本地 API。
- `pelican_tasks.py`：两种自动任务、并发及等待流程。
- `reply_capture.py`：通用助手回答提取，不依赖数字测试算法。
- `task_history.py`：持久记录和人工评价。
- `browser_manager.html`：管理页面。
- `data/browser-manager/environments.sqlite3`：环境、任务设置、记录、评价、邮箱和 DPAPI 加密密码。列表接口只返回密码是否已保存，不返回明文或密文。
- `manager_credentials.py`：Windows 当前用户 DPAPI 加密，不保存加密主密钥；其他 Windows 用户/机器不可直接恢复凭据，应重新输入。
- `manager_login.py`：受信任 Arena 域名上的自动登录和保守身份验证。
- `data/browser-manager/profiles/`、`archive/`、`task-artifacts/`：登录数据、移除环境归档、截图。

新管理器的 `numeric_tasks.py` 已删除。旧工具 `webui.py`、`main.py`、`modeltrace/` 和 `shun插件/` 保持不变，避免破坏旧项目；新管理器不再调用数字题生成、数字解析或指纹归因模块。

服务仅绑定本机，校验 Host/Origin，写请求要求 `X-Manager-Request: 1`。不提供公网认证，不要端口转发到公网。数据目录已由 `.gitignore` 排除，不自动清理，长期使用需留意磁盘空间。

API 启动路径仍兼容 `/api/environments/{id}/pelican/start`，`kind` 仅接受 `pelican` 或 `recreate`。记录查询、评价、打开对话、截图和导出接口保留；`reanalyze`、`manual-answer` 相关接口已移除。

此次改动未运行测试、未发送题目、未删除历史数据库或操作实际对话。需要重启管理器并刷新页面生效。


### 本次移除范围

删除新管理器的 `arena_thinking.py`；移除 `stop_on_thinking` 设置、运行时检测/停止/归档调用、思考诊断接口及页面按钮。旧配置中的该字段读取时忽略，不会重新启用。已保存的历史状态、诊断数据、截图和人工评价不删除，仍可随记录导出。

旧工具和原插件源文件保留但不纳入发行包；新管理器不再提供插件功能。用于保存普通回答的通用文本提取不受影响。当前不提供网站对话自动归档功能；“移除浏览器环境时归档 profile 数据”是另一项数据保护行为，继续保留。

请先保存工作并结束当前任务，再重启管理器并刷新页面，旧进程不会自动更新。未运行测试或对实际对话执行任何操作。


## Windows 内置 Chromium 分发版

### 普通使用者

收到 `Harbor-Windows-x64-时间戳.zip` 后完整解压，双击 `Harbor/Harbor.exe`。无需安装 Python、项目依赖或单独下载安装 Chromium。默认浏览器用于显示管理页；独立账号环境始终由包内 Chromium 打开，不使用系统 Chrome/Edge。

发行包是 **免安装文件夹版**，不是安装向导。`Harbor.exe` 和 `_internal` 必须一起保留。再次双击会检查本机管理器是否已运行，避免重复启动；如果源码版仍占用端口，会打开已有实例，请先关闭旧实例再体验新版。

运行中请保留控制台。正常退出先保存工作，再在控制台 Ctrl+C 等待关闭。软件未代码签名，Windows 可能显示未知发布者提示；应核实来源，而不是关闭系统安全保护。

### 独立数据目录

- 发行版默认保存到 `%LOCALAPPDATA%/Harbor`，不向程序安装目录写入。
- 源码模式继续使用项目的 `data/browser-manager`，不会自动搬走你现有的数据。
- 可通过 `HARBOR_DATA_DIR` 环境变量指定其他数据目录；不同副本不要同时使用同一数据目录。
- 发行包不包含开发者的数据库、profiles、截图、日志或原插件。每个使用者第一次启动均自行创建环境、保存邮箱和密码。
- 升级程序不删除用户目录。直接删除程序文件夹也不会删除用户资料。

### 开发者构建

双击 `构建Windows发行包.bat`，或在 Windows x64 项目目录运行：

```powershell
python build_windows.py
```

脚本会创建隔离的 `.packaging-venv`，安装 `requirements-build.txt` 中的依赖，下载匹配版本的 Chromium 到专用构建目录，再使用 PyInstaller 生成带浏览器资源的目录及 ZIP。不会修改日常 Python 环境，不使用用户的浏览器资料，也不会运行真实网页任务。

产物位于 `release/Harbor-Windows-x64-时间戳.zip`；`release/LATEST.txt` 指向最新构建。资源来源按文件白名单加入：管理页面、打包代码依赖和新下载的 Chromium，不复制整个项目目录。相关第三方许可及构建清单随包提供。

源码用户也可双击 `一键启动.bat`（仍需已有 Python/依赖）；发行版则直接双击 EXE。

新增：`runtime_paths.py`、`harbor_launcher.py`、`build_windows.py`、`requirements-build.txt` 和分发说明。版本清单记录 `runtime_validated: false`：构建成功只证明产物生成，未替代干净 Windows 机器上的启动、环境创建和浏览器验收。按现有要求，不自动执行测试或真实发送。

## 历史：上一版仅无痕自动登录说明（已被当前版本替代）

- 本次只修改主项目，发布副本与已有 EXE/ZIP 未改动、未重新打包。
- 先停止任务并关闭旧进程，重新启动源码管理器、刷新页面。编辑旧环境，保存邮箱密码后再启动。数据库仅新增凭据字段，不删除旧 profile 和任务记录。
- 编辑密码留空代表保留；更换邮箱必须重新输入密码。「清除凭据」同时清除邮箱与加密密码，之后须重新保存才能启动。移除环境会删除凭据记录，但 SQLite/备份中的物理残留不保证安全擦除。
- 密码框关闭即清空，后端校验错误不回显输入。登录异常不输出原始 Playwright 错误，不保存登录截图或页面快照。运行时仍需短暂解密密码到内存；DPAPI 不能防御同用户权限下的恶意程序。
- 每次启动总是先访问 Arena 登录页；原「启动地址」保留为登录后手动新标签页的目标，不用于向任意网站填入凭据。
- 无痕关闭丢弃会话，不删除保存在管理器中的凭据。浏览器仍受网站风控约束，无痕不等于匿名或指纹伪装。
- 未运行测试、未使用真实凭据登录、未发送题目。需要在 Windows 上人工验收 DPAPI 保存/重启解密、登录页面兼容性及身份确认接口。

### 临时环境丢弃的边界

- 自动丢弃针对已经成功创建浏览器会话的临时环境。仅创建配置但尚未启动、或在会话创建前启动失败的环境保留，便于修正后重试。
- 正常退出管理器也会关闭受管浏览器并等待清理完成。系统断电、强制终止管理器等导致关闭回调未执行时，列表可能残留，需手动删除；不声称此类情况也已完成清理。
- 删除临时环境前先停止并等待自动任务写入最终状态，不删除历史表、账号表或截图。清理失败时保留错误提示，不允许重新启动正在丢弃的环境。
- 自动展开侧边栏使用明确的 Expand/Open/Toggle Sidebar 控件以及 sidebar trigger 标识，不点击不明确的图标，不处理验证码。网站结构变化时仍可能需要人工干预。
- 本次仅做静态检查，未操作真实浏览器或账号来验证侧边栏和关闭回调。

## 登录状态识别修正（当前规则）

之前只依赖 `/api/auth/session` 的 `user.email`，这个接口未经过实机验证，接口缺失时会将已登录页面一直拦住。现在该接口仅作为一种可选证据，不再是唯一判据。

- 检查时尝试展开侧边栏，检测登录表单、验证码、安全验证和聊天输入区。必要时只打开明确标记的账号菜单，读取邮箱与账号/退出登录控件，然后关闭由管理器打开的菜单。不点击退出登录，不把聊天内容中的邮箱当作账号证据。
- 自动确认：会话接口邮箱匹配，或账号菜单邮箱匹配且有明确账号/退出登录控件。账号不匹配、登录表单或验证码仍显示时阻止任务。
- 无法自动确认：显示具体原因，不直接宣称未登录。API 不返回页面正文、Cookie、会话令牌或实际探测出的邮箱，只返回结构状态及原因。
- 页面已登录但网站不提供可识别邮箱时，用户可先在该环境的账号菜单核对邮箱，再点击 **人工确认登录**。需要二次确认目标邮箱。系统仍要求 Arena 聊天输入区可用，且未检测到登录入口、登录表单、验证码或账号不匹配；不是跳过所有检查。
- 人工确认标为「人工确认已登录」，与自动识别分开；仅绑定当前 BrowserContext，有效 10 分钟，关闭环境即失效。任务开始前仍检查状态，明确未登录或账号不匹配会撤销确认。超时后需要再次人工确认，不自动中断已经开始的任务。
- 人工确认后不会自动发送题目，仍需用户在自动任务面板授权开始。本轮任务结束后按原逻辑保存记录。
- 修改源码后需要重启管理器并刷新页面。此次仅做静态检查，未使用真实账号或运行网页任务；页面选择器仍可能需要根据现场结构调整。

## 本次界面与头像邮箱识别修正

- 管理器常用字体与按钮略微加大：按钮和表单约 15px、行内容 15px、说明文字 14px，按钮高度至少 42px。
- 取消所有操作前的浏览器确认弹窗，包括删除账号/环境、关闭临时环境、批量关闭、停止任务和人工确认。按钮点击即执行；输入账号和任务参数的编辑表单仍保留，错误及结果使用页面提示。
- 开始任务不再额外勾选授权框；点击「开始任务」即授权本轮发送。人工确认按钮点击即表示用户已核对该环境账号，仍保留验证码、未登录和明显账号不匹配的服务端拦截。删除操作不可恢复，不再二次询问。
- 根据当前截图，增加侧边栏“头像＋邮箱账号区”的识别，不再要求一定存在 Account menu/Log out 文案。优先读取完整 DOM 文本、title、aria-label；CSS 省略号通常不影响完整文本读取。
- 只有文本本身已被截断时才使用前缀规则：需匹配完整邮箱本地部分及域名前缀，同时有头像、侧栏/左下账号区位置、可用聊天输入区，且没有登录表单、验证码和冲突邮箱。此时页面明确提示“完整邮箱未核验”，不把前缀匹配声称为完整身份验证。
- 自动登录结束后若状态仍不确定，每 5 秒进行一次只读状态观察，检测到登录后自动更新管理器，无需再次点人工确认；运行中的任务不触发此观察。观察不点击菜单、不发送内容，环境关闭或管理器退出时取消。
- 此次读取了当前管理器状态，确认其原状态为“输入区可用但身份识别不确定”；新增账号区规则依据用户截图实现，尚未在真实 DOM 上运行验证。未重启管理器、未关闭当前浏览器、未发送题目、未重新打包 EXE。

## v0.3.1：环境编号与排序

- 新创建的环境在环境列表最上方显示，同一创建时间按后插入者优先。账号列表顺序不变。
- 同名创建时，第一个保留原名，后续使用 `原名 (2)`、`原名 (3)`，跳过已占用编号。名字比较忽略大小写和首尾空格，生成后不超过 80 个字符。
- 编号依据当前存在的环境分配；删除环境后空出的名字或编号可能再次使用。不会重命名已有环境，也不修改历史任务快照。
- 账号页“新建并启动”和环境页“新建环境”都由同一后端创建方法处理，分配名字和写入在同一数据库事务内进行。
- 账号列表新增备注列，内容直接展示并自动换行。

## v0.3.2：关闭任务页后继续补位

- 手动关闭正在执行的任务标签页，只结束该题的本地跟踪并释放名额；存在剩余任务且环境仍打开时，等待设置的间隔后继续下一题，不会重新发送被关闭的题目。
- 等待输入锁、发送间隔、提交确认及回复期间关闭标签均会及时退出该题；已保存的回复保留。关闭页面不保证服务端停止生成。
- 关闭已完成的旧任务页不会影响正在运行的其他题目。
- 关闭整个环境（包括最后一个浏览器标签）或点击停止，仍停止整轮；不会自动重开环境。没有待执行题目时不再新增标签。其他异常仍保守停止。

## v0.3.3：火柴人大战

自动任务新增「火柴人大战（SVG / SMIL 或 CSS）」，沿用数量、间隔、并行及关闭标签后补位设置，保留原有题目。新题原文：

请使用纯 SVG 和原生 SMIL 动画（或纯 CSS），制作一个"火柴人大战"的完整 2D 动态页面。

## v0.3.4：内置试题介绍与效果图

打开「自动任务」，选择题目后会同步展示介绍、观察要点及参考效果；点击「展开大图」在面板内查看细节，不弹出新窗口。

- 鹈鹕：并排展示结构较完整与角色/车辆脱节的两个示例。来源未标明模型身份，示例提示词与软件当前题目不完全相同。
- 火柴人：展示原文的 SVG / SMIL 战斗动画，观察出招、闪避和反击的时序。原文没有模型名称或弱例，不补造。
- CSS 复刻：保留一张原图加五个模型结果的整张横评图及其原始标注；图示来自来源文档注明的 JackCui 转载内容，版本、参数、生成条件未独立核验。实际参考图以软件发送题目的链接为准。

图片以数据 URI 内置在管理页面，随 EXE 打包，无需联网加载。来源链接需要联网。来源：[ShunCode · 抽卡判断](https://docs.shuncode.top/docs/advanced/gacha/)，整理日期 2026-09-11。火柴人 SVG 从原站动画页面提取并携带原 CSS，保留 SMIL 动画。

这些是第三方参考示例，不属于 Harbor 实测，不用于自动认定模型身份、排名或保证代码能力。运行结果仍需在任务记录中人工审核。本次只更新介绍展示，不修改发送题目、任务调度或用户数据。

## v0.3.5：鹈鹕测试快速版

新增独立题目，不替换普通鹈鹕。选择后展示完整提示词、直接编码要求与沿用普通题的参考效果图。题目中的文件路径是目标模型工作区路径，不是 Harbor 本机路径。提示词不能保证模型不进行内部推理；本版不加入 Thinking 自动停止或视觉自动淘汰。

```text
这是一个编码能力测试，不是分析或规划测试。本题不允许思考、分析、规划或讨论方案，请看到需求后立即开始创建文件、编写代码。

任务：创建一个 HTML，内容是用 SVG 绘制“鹈鹕骑自行车”的 2D 动画。

执行要求：

1. 第一项操作立即调用 functions.write_file，在 /home/user/index.html 中直接写入完整 HTML 代码，不要先输出任何文字。
2. 不要检查目录、读取文件、搜索、安装依赖、执行命令、测试、截图或验证。
3. 不要询问问题，所有细节自行决定，直接编码。
4. HTML、CSS、SVG 和 JavaScript 全部内联，不使用外部资源。鹈鹕须有明显的长嘴与喉囊，动画包含车轮旋转和双腿踩踏。
5. 写入完成后立即调用 functions.present_file 打开 /home/user/index.html，然后结束，不追加解释。

注意：这是编码能力测试，不允许先进入 Thinking 或输出思考过程。现在立即调用文件写入工具，开始编码。
```

同电脑升级继续使用原数据目录，账号、密码、备注和历史无需重新导入。本次不改数据库结构；本地 `experiments/gacha_probe` 实验文件不纳入发布仓库或安装包。

## HTML 结果陈列（本地开发版）

审核页默认展示 HTML 动态陈列，可切回全部任务记录。所有账号合计保留最近 100 个成功采集的 HTML 结果，按采集完成时间排序；第 101 个进入后，删除最旧 HTML、对应任务记录（含评分/收藏/备注）及截图。收藏不豁免。非 HTML 或采集失败的任务不计入这个上限，也不会因此被删除。

- 点击卡片可查看账号名称/邮箱快照、环境、题目、完成时间、来源、大小和审核信息，不保存密码。新任务启动时固定账号快照，删除环境后仍能查阅。
- 只保存取得的完整 HTML 文档：当前回复代码块、该回复内的 srcdoc、受限的同源文件下载链接或已经保存的回答代码。无法取得源文件时明确标记，不把聊天页面截图或整页 DOM 冒充结果。当前不会操作文件查看器按钮，不保证适配所有 Arena 文件交付形式。
- 单个 HTML 上限 2 MB；每个任务保存一个结果。原始 HTML 存入本机数据库并可下载，不重新发送旧任务。本版不自动回填旧记录。
- 预览为无权限 sandbox，并禁用脚本、外网、表单和嵌套页面。SVG/SMIL 与 CSS 动画可播放；JavaScript 动画、外部字体/图片可能无法显示。为严格避免预览访问账号或外网，不能在管理器里运行任意生成脚本。原文件仍完整保留，可自行下载至可信环境查看。
- 卡片进入可见区域时才加载预览，离开区域卸载，避免同时运行 100 个动画。点击只打开详情，不触发题目发送。
- 首次建立陈列表前自动备份现有 SQLite 数据库，继续使用原同电脑数据目录，不修改账号表。HTML 正文及任务记录删除在一个事务中完成，截图失败删除会进入重试队列；最小删除标记防止仍在内存中的旧任务把记录重新写回来。备份可能保留已删除的数据，不承诺安全擦除。

本地开发阶段：须通过临时数据库和模拟页面验证；不直接操作真实账号或现有记录，不自动发布 GitHub。

## 工作空间 HTML 自动捕获（待重新加载的本地源码）

任务页创建后安装独立文件监听，点击发送前才启用；捕获实际出现的 `ws-*.arena.site` HTML 响应和该页面 HTML 下载。未捕获到响应时，尝试读取当前页面已经提供的工作空间 HTML 链接或 iframe 地址，不猜路径、不构造访问凭证、不自动点下载按钮。

采集器使用的工作空间请求地址只保存在任务内存中，不额外写入记录元数据或日志。原 HTML 内容按原样保留，其中可能包含平台自身注入的资源引用。补读不携带浏览器 Cookie/Authorization，拒绝重定向，保留 2 MB 文档限制。完成后依次尝试新通道和原有采集方式；成功才送入全账号最近 100 条陈列。停止、关闭或完成任务均移除监听，不监听其他任务后续输出。

如果文件只有点击下载后才暴露地址，且自动任务期间从未加载或展示该 HTML URL，本版仍可能无法自动取得；会保留普通记录和明确的采集失败说明，不把手动下载验证冒充无人值守验收。

本地开发改动，尚需重新加载程序生效；不自动重启正在使用的环境，不发布 GitHub。


## YesCaptcha 可选插件与150行候选（本地源码已接入，待重新加载）

### YesCaptcha

浏览器环境列表中，保留型环境提供「YesCaptcha · 关闭/已配置开启」按钮。默认关闭，临时无痕环境不提供入口，后端也拒绝启用。运行中可以保存插件设置，但只在该环境下次启动时生效，不重启、不热切换。

用户从官方渠道取得完整、已解压的 Manifest V3 YesCaptcha 插件目录，填写包含 manifest.json 的绝对路径后开启。当前程序不内置或自动下载第三方插件，不支持直接选 CRX/ZIP 文件。官方商店链接已放在设置中；可以在用户自行安装官方扩展后指定对应版本目录，Harbor 不扫描其他浏览器资料。

目录仅做名称、Manifest、后台脚本等结构检查，不代表官方签名认证。只加载所指定扩展；关闭时保留原有禁用扩展行为。插件移动或缺文件会阻止启用启动并提示原因，不悄悄退回无插件模式。设置中不收集 ClientKey，用户在该环境的插件内自行处理密钥与积分。插件状态「请求加载」不代表已经配置密钥或验证码已解决。

配置放在同电脑原数据库的新 yescaptcha_settings 表，独立于旧 extension_enabled 字段。首次由新代码加载时，在已取得管理器数据锁、旧事务提交后先 SQLite backup，再建表；备份失败不继续迁移。备份位于现有数据目录 migration-backups，不进入发布包。删除环境时一起删除插件设置；测试未迁移或修改当前真实数据库。

### 150行候选

新成功捕获的完整 HTML 同时保存原始行数、非空行数、最长行和候选标记，并在成果卡片与详情中展示。鹈鹕/快速鹈鹕 ≤150行（含150）为候选；>150仍保留，长行给压缩提示。不是模型身份证明。不格式化或去除平台脚本来改变计数，旧记录显示「未统计」，不会补发旧任务。

### 本轮验证与剩余事项

14项插件后端测试、6项生产候选规则测试、22项自动采集/陈列/标签调度回归测试通过；另有6项模拟网页交互检查通过。测试使用临时数据库、模拟 API 和隔离 Chromium，未安装真实 YesCaptcha、未消耗积分、未操作当前对话。测试依赖 httpx 安装于开发打包虚拟环境，未加入生产 requirements。

源码修改未自动重载，未重新打包或发布。仍需在用户方便时加载新源码，完成真实插件、无需手动下载的自动入柜验收。Thinking立即中断和限流/验证码恢复策略仍是隔离实验，尚需真实页面信号校准与生产调度接入，不把离线策略测试当作已上线功能。
