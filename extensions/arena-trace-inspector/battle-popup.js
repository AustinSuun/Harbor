import './battle-core.js';
/* Battle history is intentionally separate from Agent run/span totals. */
const section=document.createElement('section');section.className='history';
const title=document.createElement('h2');title.textContent='Battle 官方揭示记录';
const note=document.createElement('p');note.className='note';note.textContent='官方揭示与调用数据独立保存；Token / 成本来自 Battle 消息元数据，不计入 Agent trace 累计。';
const list=document.createElement('div');section.append(title,note,list);document.querySelector('.privacy').before(section);
async function refresh(){try{const r=await chrome.runtime.sendMessage({type:'ATI_BATTLE_LIST'});if(r?.error)throw Error(r.error);list.replaceChildren();for(const record of r.records||[]){const card=document.createElement('article');card.className='record';const a=document.createElement('a');a.href=record.url;a.target='_blank';a.rel='noopener noreferrer';a.textContent='Battle · '+record.sessionId;card.append(a);for(const pair of record.pairs){for(const m of pair.sides){const p=document.createElement('div');p.className='model';p.textContent=m.position.toUpperCase()+' · '+m.displayName;card.append(p);const v=ArenaBattleCore.callDataView(m.callData,true);const note=document.createElement('small');note.textContent=v.source;card.append(note);for(const [label,value] of v.rows){const line=document.createElement('div');line.textContent=label+'：'+value;line.style.fontSize='11px';card.append(line);}if(v.warning){const warning=document.createElement('small');warning.textContent=v.warning;card.append(warning);}}}const time=document.createElement('small');time.textContent='本地历史 · '+new Date(record.observedAt).toLocaleString();card.append(time);list.append(card);}if(!r.records?.length)list.textContent='暂无 Battle 记录。评分揭示后会自动读取；失败时可在 Battle 浮层重试。';}catch{list.textContent='Battle 记录读取失败，请重新加载扩展';}}
chrome.storage.onChanged.addListener((changes,area)=>{if(area==='local'&&Object.keys(changes).some(k=>k.startsWith('ati.battle.v1.')))void refresh();});void refresh();
