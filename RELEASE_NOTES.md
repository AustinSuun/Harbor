# Harbor v0.3.11 — 默认双插件、统一密钥、运行中备注

Windows x64 预览发行，未代码签名。

## 使用

- 启动环境即加载 Trace Inspector 与 YesCaptcha，不再逐个环境打开开关或填写目录。
- 进入 **浏览器环境 → 插件与密钥**，填写一次 YesCaptcha ClientKey 并保存。所有环境下次启动自动使用；保存/清空不重启当前环境，当前旧配置继续有效直到关闭。
- Trace Inspector 仍需在插件浮层手动开启监听，不会自动抽卡。不要同时运行插件抽卡与 Harbor 自动任务。
- 账号有环境运行时，可在 **更多 → 编辑账号** 修改名称和备注；不会打断任务，登录凭据仍禁止运行时修改。

## 安装与隐私

Trace Inspector 2.0.0 已内置，使用固定扩展ID及运行文件哈希校验。旧自定义目录版记录不自动迁移，原资料不删除。

YesCaptcha 1.4.7 首次启动环境时从官方自动下载并校验固定SHA256，随后复用本机缓存。首次需要网络；下载/配置失败会停止本次启动并提示重试。YesCaptcha 官方代码不在本仓库或ZIP中再分发。

ClientKey 在管理器内加密保存，不写入源码、日志或分发包；扩展运行时自身本机存储也含敏感配置。未配置密钥时关闭自动解题，设置有效密钥后可能消耗付费点数。扩展声明 all_urls、management 等较广权限；本次不是完整第三方安全审计或官方背书。

## 临时环境说明

为支持插件，临时环境采用一次性独立 Chromium profile，不是原生无痕窗口。正常关闭会删除目录和环境项，账号与历史保留。异常崩溃或断电可能留下 temporary-plugin-profiles 残留，需要确认浏览器退出后清理。

## 验证边界

验证统一密钥加密/不回传、无密钥停用自动解题、全局界面、运行中备注、隔离离线双插件加载与临时目录清理。真实 Arena trace、Thinking/完成检测和实际生成并行数仍未校准；本版不宣称修复这些检测问题。不进行真实账号登录或付费解题测试。

## 升级

保存工作并自行退出旧管理器，再完整解压新ZIP运行 Harbor.exe。仅刷新网页不等于退出后台。账号、备注、历史和保留型资料继续使用本机数据目录。未自动安装或重启你的管理器。
