# Harbor v0.3.2 — 任务标签关闭补位修正版

## 修复
- 关闭单个正在执行的任务标签，不再误停整轮及其他并行任务。
- 有剩余任务且浏览器环境仍打开时，释放名额并在设置的间隔后继续下一题；已关闭题目不重发，不增加本轮总题数。
- 覆盖页面加载、等待输入锁/发送间隔、提交确认、生成回复时的关闭；已保存原文和已完成结果保留。
- 旧的已完成标签不再绑定当前工作任务；关闭旧标签不影响新任务。
- 关闭整个环境、最后一个标签或点击停止仍停止整轮；其他异常仍保守停止。关闭标签不代表服务端停止生成。
- 保留 v0.3.1 的环境自动编号、最新环境置顶和账号备注列。

## 升级
下载并完整解压 ZIP，保留 `_internal`，启动 `Harbor.exe`。
请先保存工作并完全退出旧管理器，否则启动器可能复用旧实例；退出时临时环境会被丢弃。
本机 `%LOCALAPPDATA%/Harbor` 的账号、备注和历史不随安装包上传；不需要再次导入账号。

## 验证范围
完成模拟异步调度测试、静态检查、Windows 构建及压缩包校验；未执行真实登录、真实 Arena 自动任务或干净 Windows 机器端到端验收。预览版，未代码签名。
