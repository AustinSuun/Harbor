# Harbor v0.3.9 — 右上角更新入口与 Thinking 只读检测

预览发行，未代码签名。Windows x64 ZIP；Mac 仍仅提供源码启动支持。

## 本次变化

- GitHub、检查更新、当前版本与更新提示移至主内容区右上角；GitHub 使用内嵌 SVG 图标，无外部图标依赖。小窗口可换行，原有更新按钮绑定保留。
- 新增侧栏 Thinking 只读检测页：选择已打开的 Agent 页面，记录界面状态元数据，对比现有规则；人工标记可见 Thinking、思考结束、回复完成或不确定。
- 支持单次采样及每秒采样（每次最长两分钟），最多3组会话、每组120条样本，仅驻留内存，可导出 JSON。
- 不发送提示、不停止生成、不归档、不改变配置。不保存/导出提示词、推理正文、完整URL、消息ID或Cookie；导出诊断前仍请自行检查内容。

## 验证与边界

本次诊断页已通过9项合成浏览器/API/隐私/内存上限测试；右上角布局通过1440、900、390像素宽度检查和模拟更新按钮绑定检查。发布前另进行回归、Windows ZIP完整性及隐私路径审计、独立EXE自检。

**尚未采集并校准真实 Arena 的 Thinking 阶段。本版是增加诊断能力，不是宣称修复 Thinking 识别。** 候选标签、动画或停止按钮不能证明内部推理或模型身份。实验自动停止与自动归档继续默认关闭，使用诊断页前请自行保持关闭。

未完成真实登录、干净Windows机器、实际更新安装交接、真实Mac和付费验证码测试；不将合成测试当作实站验收。

## 升级

可在旧版“检查更新”中获取此预览版。手动升级请先保存工作并退出旧管理器（不只是关闭网页），完整解压ZIP，再运行Harbor.exe。账号、备注、历史与保留型资料继续使用本机 `%LOCALAPPDATA%/Harbor`；临时环境退出会丢弃。

一键更新须先关闭所有环境，不强制终止任务。包与SHA256来自 AustinSuun/Harbor，校验通过后旁路安装，保留旧程序。SHA256不是发布者数字签名。

不上传账号、凭据、浏览器资料、私人成果、真实诊断报告。Mac用户请下载源码并阅读MACOS.md；不提供原生Mac安装包。
