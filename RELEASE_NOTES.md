# Harbor v0.3.10 — 界面重设计、插件接入与并行设置

Windows x64 预览发行，未代码签名。Mac 仍仅提供未经真实 Mac 验收的源码启动支持。

## 本次更新

- 清爽浅色主界面与青绿色导航，统一按钮、间距、弹窗与键盘焦点。
- 账号新增搜索、概览和空状态；备注完整换行。环境支持状态/类型/账号组合筛选，次要操作集中到“更多”。
- 批量操作仅针对当前筛选中选中的环境；账号快捷入口使用精确账号ID，新建环境后清除旧筛选。
- 任务弹窗按题目、运行设置、进度和实验选项分区；全局连接错误、小窗口卡片布局、密码显示切换与关闭清理。
- 保留型环境新增 Trace Inspector 独立开关与目录配置，默认关闭，可与 YesCaptcha 合并加载，下次启动环境生效。**只提供加载支持，原插件不随包分发，须自行提供完整解压目录。**
- 取消并行数最多3个的固定上限；允许正整数，实际调度数最多为本轮任务数，总任务数仍为1–50，发送间隔与停止保护不变。

## 重要边界

**本版没有修复或校准真实 Arena 的对话完成检测、Thinking 识别和实际生成并行数。** 界面“调度占用”包含等待与提交；“本地结束”不等于成功完成。不要把界面重设计或取消并行上限当作检测修复。

Trace Inspector 使用 activeTab/debugger/storage 权限并访问 Arena 与 Trigger.dev。Harbor 不自动开启监听、发送、重命名或归档，也不重置插件已有偏好；请自行检查原插件设置，勿同时运行插件抽卡与 Harbor 自动任务。临时无痕环境暂不支持。声明权限/入口校验不等于签名或完整安全审计，真实 trace/模型标签/余额未在本次验证。

Thinking 自动停止、自动归档和两个插件加载开关继续默认关闭。首次新增插件设置表前备份本机数据库。

## 验证

16组模拟UI场景通过，覆盖筛选、批量隐藏项隔离、密码、连接恢复、任务参数和题目参考；检查1440/1024/768/390宽度。Trace Inspector 17项后端、6项模拟界面检查以及一次性离线Chromium加载/debugger与Playwright兼容性检查通过。并行设置5项、标签关闭补位7项、Thinking诊断9项和旧插件/更新UI检查通过。发布前另检查分发包完整性、版本、内嵌模块、用户数据排除及独立EXE自检。

这些测试不等于干净Windows机器、真实登录、网页任务、实际更新安装交接、真实Mac或付费验证码验收。

## 升级与隐私

旧版可点击“检查更新”获取此预览发行。升级前保存工作并退出旧管理器，不只是关闭网页；一键更新须先关闭所有环境。临时环境退出会丢弃，账号、备注、历史及保留型资料沿用本机 `%LOCALAPPDATA%/Harbor`。

手动升级请完整解压 ZIP，再启动 Harbor.exe。SHA256用于完整性校验，不是发布者数字签名。不上传账号、凭据、浏览器资料、私人成果、插件原文件或真实诊断报告。
