# Harbor v0.3.5 — 鹈鹕测试快速版

- 新增独立的「鹈鹕测试快速版」，保留原有三个题目。
- 完整发送用户提供的直接编码提示词：第一步写入 /home/user/index.html，随后展示文件，不先讨论方案或执行测试。
- 选择题目时展示完整提示词、执行要求及普通鹈鹕参考图（非快速版实测）。任务摘要、历史名称和保存的类型同步支持新题。
- 数量、间隔、并行、关闭标签后补位行为不变。
- “不允许思考”仅为提示词要求，不保证内部推理行为，也不触发自动停止。
- 本地自动评审实验仍未接入；不随源码发布或安装包分发。

## 升级
先保存工作、完全退出旧管理器，再完整解压并启动 Harbor.exe，保留 _internal。同电脑沿用原账号、密码、备注与历史；无数据库结构变更，不需要重新导入。临时环境退出即丢弃。

## 验证
完成类型与题目一致性、界面语法、模拟调度回归及 Windows 包校验。未执行真实 Arena 发送或登录验收。预览版，未代码签名。
