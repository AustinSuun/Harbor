# Harbor v0.3.1 — 环境编号与备注展示预览版

## 本次更新
- 同名创建环境时自动添加 `(2)`、`(3)` 等数字编号，跳过已有编号；名称比较忽略大小写和首尾空格。
- 新创建的环境显示在列表最上方；不重命名已有环境，账号列表顺序不变。
- 账号列表新增备注列，长备注自动换行，无需打开编辑窗口。
- 保留 v0.3.0 的账号管理首页、精简界面、加密凭据、两类浏览器环境与自动任务功能。

## 下载与升级
下载 `Harbor-Windows-x64-*.zip` 并完整解压，双击 `Harbor.exe`。不要只复制 EXE，必须保留 `_internal`。
升级前保存工作并退出旧管理器，再启动新版；否则启动器可能打开旧实例。退出时临时环境会被丢弃。
用户账号及备注保存在本机 `%LOCALAPPDATA%/Harbor`，升级不会将它们打进包内或上传到 GitHub。
编号以当前存在的环境为准，删除后空出的名字或编号可能复用。

## 验证范围
预览版。完成编号逻辑的内存数据库检查、静态检查、Windows 构建及压缩包内容校验；未执行真实登录、自动任务或干净 Windows 机器端到端验收。程序未代码签名。
