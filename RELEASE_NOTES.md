# Harbor v0.3.0 — 账号与环境管理预览版

## 更新
- 启动默认进入账号管理；精简界面说明，只保留操作引导和必要状态。
- 账号与环境分离，凭据使用 Windows DPAPI 加密保存。
- 保留型环境可重开；临时无痕环境关闭后自动丢弃，账号及历史任务保留。
- 自动展开侧边栏，增加头像邮箱检测、大小写与显示字符规范化、登录状态观察和人工确认入口。
- 字体和按钮加大；删除、关闭、人工确认和任务操作点击即执行，无二次弹窗。
- 保留鹈鹕动画、HTML/CSS 复刻、任务记录和人工审核功能。

## 下载与升级
下载 `Harbor-Windows-x64-*.zip` 并完整解压，双击 `Harbor.exe`。不要只复制 EXE，必须保留 `_internal`。
升级前保存工作并退出旧管理器，再启动新版；否则启动器可能打开仍在运行的旧实例。退出时临时环境会被丢弃。
包内不包含开发者账号、数据库、浏览器资料、截图或日志。
删除环境不可恢复；账号及历史记录按操作规则保留。

## 验证范围
预览版。完成源码静态检查、Windows 构建与压缩包内容检查；未完成干净 Windows 机器运行、真实账号登录及自动任务端到端验收。登录控件和检测规则可能需要随网站变化调整。
程序未代码签名；使用下载附件中的 SHA-256 文件校验 ZIP 完整性。
