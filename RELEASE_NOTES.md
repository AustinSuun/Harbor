# Harbor v0.3.3 — 新增火柴人大战测试题

- 自动任务新增「火柴人大战（SVG / SMIL 或 CSS）」选项，预览及实际发送均使用用户提供的完整题目原文。
- 原有鹈鹕动画、HTML/CSS 复刻题目保留；新题支持数量、间隔、并行、结果保存和关闭标签后补位。
- 环境任务摘要和历史记录正确显示新题名称。

## 题目原文
请使用纯 SVG 和原生 SMIL 动画（或纯 CSS），制作一个"火柴人大战"的完整 2D 动态页面。

## 升级与验证
完整解压 ZIP，保留 `_internal`。先保存工作并完全退出旧管理器，再启动新版 Harbor.exe，避免复用旧实例。临时环境退出即丢弃；账号、备注和历史保留在本机，不上传。
完成题目映射/前端语法检查、模拟调度回归、Windows 构建和包校验；未执行真实 Arena 发送或登录验收。预览版，未代码签名。
