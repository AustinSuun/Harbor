# Harbor v0.3.12 — Trace Inspector 更新至 2.3.0

Windows x64 预览版，未代码签名。

## 本次更新

- 按用户指定目录的版本，将内置 Arena Trace Inspector 从 2.0.0 更新为 **2.3.0**。
- 保持 v0.3.11 的扩展公钥和扩展ID，避免仅因更新版本或解压路径变化而切换扩展身份。不主动删除或重置现有插件记录与偏好。
- 仅收录34个运行文件，新增 catalog.js；不包含 probe、上游测试、抓取记录、研究说明或私人账号数据。原插件目录不修改。
- 保留双插件默认加载、YesCaptcha 全局加密密钥和运行中修改账号备注。

## 插件行为提醒

上游 2.3.0 默认开启自动重命名（已有显式关闭偏好仍保留）；手动启动抽卡后，抽卡结束不再自动停止监听。Harbor 不替用户启动监听或抽卡。不要同时运行插件抽卡与 Harbor 自动任务。

本次保留上游关于模型字段、目录查询和额度展示的实现，不把字段标签当作模型权重证明，也不复述上游研究记录为本次验收结论。真实 trace、额度正确性、Thinking/完成及并行检测仍需实际页面校准。

## 使用与升级

保存工作并自行退出旧管理器，完整解压新ZIP运行 Harbor.exe。现有环境需关闭重开才使用新插件，不对当前运行浏览器热替换。

环境页“插件与密钥”统一设置 YesCaptcha ClientKey，所有环境下次启动应用。YesCaptcha 仍在首次启动时从官方自动下载并验证固定SHA256，需联网，不在本仓库/ZIP再分发；未配置密钥时关闭自动解题，配置有效密钥后可能消耗点数。

临时隔离环境使用一次性 Chromium 资料目录，不是原生无痕窗口。正常关闭删除资料，崩溃可能有残留。插件存储可能包含敏感数据，请勿共享profile。

本次不安装或重启当前管理器，不使用真实账号或运行付费解题测试。
