# Harbor v0.3.8 — 结果陈列、实验归档与更新支持

预览发行，未代码签名。Windows 包，另提供 Mac 源码启动支持；不是 Mac 原生安装包。

## 新增与修复

- HTML 工作空间自动捕获、全账号最近100条结果陈列和审核，150行候选标签。
- 修复单页导航/页面重建异常直接取消整个队列的代码路径；其他并行任务及后续任务可继续。保留浏览器关闭、写入失败等整轮停止保护。真实手动归档的全部触发情况仍待复核。
- 默认关闭的 Thinking 停止与自动归档实验：确认停止后处理 Thinking 对话，或保存完整HTML后处理超过150行的鹈鹕对话。本地结果不删除；未确认停止、菜单不明确或归档未确认时不盲目操作。
- 保留型环境的可选 YesCaptcha 本地插件加载，默认关闭；用户自行提供官方解压目录、配置密钥和积分。
- GitHub 链接、更新提醒、Windows 一键更新。识别本仓库数字版本的预览发行，显式点击才安装；有环境运行时拒绝更新，不强制结束任务。
- Mac 源码启动脚本和 macOS 钥匙串凭据引用；仅做脚本语法与模拟钥匙串测试，真实 Mac 未验收。

## 验证

65项后端/规则/回归测试、31项Thinking相关模拟检查、6项网页设置检查通过；包含模拟页面上的“检测Thinking→请求停止→确认界面空闲→归档确认”集成用例。另进行 Windows ZIP 完整性、模块与隐私文件排除检查，以及独立 EXE 自检（不启动管理器、不使用账号）。

这些结果不等同于真实 Arena 的菜单/状态选择器已校准；实验可能报告不支持，而不是实际归档。真实验证码、付费调用、真实Mac及完整新版本安装切换均未验收。

## 升级与数据

首次从旧版升级：保存工作并退出旧管理器，完整解压新包并启动。保留型账号资料沿用 `%LOCALAPPDATA%/Harbor`，不要重新导入私人数据库到源码。首次新建陈列/插件设置表前自动备份。

本版的一键更新会下载、校验并旁路安装，不覆盖原包或账号目录。须先关闭所有环境；临时环境关闭会丢弃。校验失败保留旧程序。SHA256不是代码签名。

Mac 请使用源码压缩包并阅读 MACOS.md。Windows ZIP 不能在 Mac 直接运行。

本次经授权公开所需实验适配器与合成测试；不上传账号、密码、浏览器资料、抓取的私人成果或实验运行输出。
