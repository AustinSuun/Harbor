# macOS 源码启动（预览支持）

此版本提供源码服务启动与 macOS 钥匙串凭据后端，不提供已经验收的原生 .app / DMG。
当前开发与测试环境是 Windows；Mac 启动、真实钥匙串授权与浏览器流程尚未在真实 Mac 上验收。

## 启动

1. 安装 Python 3.11 或更新版本。
2. 下载此项目对应版本的 Source code ZIP，完整解压到可写目录。
3. 在终端进入项目目录，运行：

```bash
chmod +x start-macos.command
./start-macos.command
```

脚本创建 `.venv-macos`，安装依赖和 Chromium，再启动本机服务 `http://127.0.0.1:8766`。
首次运行需要联网下载依赖和浏览器。不要使用 sudo。
默认数据目录为 `~/Library/Application Support/Harbor`；后续同机更新继续使用此目录。
关闭时先保存浏览器工作，再在终端按 Ctrl+C；临时无痕环境退出即丢弃。

## 凭据

- Mac 使用系统钥匙串，不回退到明文文件。首次保存/读取可能请求系统授权。
- 本地数据库仅保存随机钥匙串引用，不保存明文密码。
- Windows DPAPI 凭据不能直接在 Mac 解密；须重新保存账号密码。此版本不提供跨电脑迁移。
- 钥匙串失效或授权拒绝会明确失败，不尝试其他不安全存储后端。

## 更新与插件

Windows ZIP 内的一键更新不用于 Mac 源码服务。Mac 更新源码前退出旧服务，替换代码后再次运行脚本，保留上述用户数据目录。
YesCaptcha 当前只开放给保留型环境，用户提供官方 Manifest V3 解压目录；Mac 扩展实际兼容性仍待验证。

## 已做与未做

已做：启动脚本 bash 语法检查、模拟钥匙串保存/读取/拒绝测试、Windows 回归。
未做：Mac 原生打包、代码签名、公证、真实 macOS 运行及真实 Arena 验收。
